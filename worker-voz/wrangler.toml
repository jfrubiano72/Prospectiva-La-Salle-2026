name = "voz-lasalle"
main = "src/index.js"
compatibility_date = "2026-08-01"

# La voz se fija aqui para poder cambiarla sin tocar el codigo.
# Se define despues de escuchar el catalogo en /voces.
[vars]
# La voz ya no se fija aqui: se escoge por peticion, entre las de la lista del codigo.

# ELEVENLABS_API_KEY va como secreto, nunca en este archivo.
